# Si el backend NO corre en localhost:8080 (o en producción vive en
# otro dominio), descomenten esta línea y apunten a la URL real.
# Si la dejan vacía/comentada, en desarrollo se usa el proxy de Vite
# (ver vite.config.js) que ya apunta a http://localhost:8080.
# VITE_API_URL=http://localhost:8080
